# 部署指南

## 📁 最终项目结构

```
screen-tester-mvp/
├── index.html              # 官网首页（GitHub Pages 入口）
├── tester.html             # 核心测试页面（共享）
├── tester.css              # 样式文件（共享）
├── tester.js               # 测试逻辑（共享）
├── manifest.json           # Chrome 插件配置（根目录）
├── icon16.png             # 插件图标 16x16（需要准备）
├── icon48.png             # 插件图标 48x48（需要准备）
├── icon128.png            # 插件图标 128x128（需要准备）
├── ICONS_README.md         # 图标说明文档
├── DEPLOYMENT_GUIDE.md    # 本部署指南
└── extension/              # Chrome 插件专用文件夹
    ├── popup.html          # 插件弹出菜单
    └── popup.js            # 菜单逻辑
```

---

## 🚀 GitHub Pages 部署步骤

### 1. 初始化 Git 仓库

```bash
git init
git add .
git commit -m "Initial commit: Screen Tester MVP"
```

### 2. 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 创建一个新仓库（例如 `screen-tester`）
3. 不要初始化 README、.gitignore 或 LICENSE

### 3. 推送到 GitHub

```bash
git remote add origin https://github.com/你的用户名/screen-tester.git
git branch -M main
git push -u origin main
```

### 4. 开启 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**
4. 在 **Build and deployment** 部分：
   - Source: 选择 `Deploy from a branch`
   - Branch: 选择 `main` 分支和 `/ (root)` 目录
5. 点击 **Save**

### 5. 访问你的网站

等待 1-2 分钟后，你的网站将在以下地址可用：
```
https://你的用户名.github.io/screen-tester/
```

---

## 📦 Chrome 插件打包步骤

### 方案一：本地开发测试

1. 打开 Chrome 浏览器，访问 `chrome://extensions/`
2. 开启右上角的 **开发者模式**
3. 点击 **加载已解压的扩展程序**
4. 选择项目根目录 `screen-tester-mvp`
5. 插件就会出现在浏览器工具栏中！

### 方案二：打包为 .zip 上传到 Chrome Web Store

#### 方法 A：手动打包

1. 在项目根目录下，选中以下文件和文件夹：
   - `manifest.json`
   - `extension/` 文件夹
   - `tester.html`
   - `tester.css`
   - `tester.js`
   - `icon16.png`、`icon48.png`、`icon128.png`

2. 右键 → 发送到 → 压缩(zipped)文件夹
3. 重命名为 `screen-tester-extension.zip`

#### 方法 B：使用命令行（PowerShell）

```powershell
# 在项目根目录执行
Compress-Archive -Path manifest.json, extension/, tester.html, tester.css, tester.js, icon*.png -DestinationPath screen-tester-extension.zip -Force
```

#### 方法 C：使用命令行（Git Bash / macOS / Linux）

```bash
# 在项目根目录执行
zip -r screen-tester-extension.zip manifest.json extension/ tester.html tester.css tester.js icon*.png
```

### 上传到 Chrome Web Store

1. 访问 https://chrome.google.com/webstore/devconsole
2. 注册开发者账号（需要支付 $5 一次性费用）
3. 点击 **New Item**
4. 上传你的 `screen-tester-extension.zip` 文件
5. 填写商店列表信息：
   - 标题：Screen Master - Dead Pixel Tester
   - 描述：详细的功能说明
   - 截图：准备一些展示截图
   - 分类：选择合适的分类
6. 提交审核

---

## 🎨 图标准备

在部署之前，请准备以下三个尺寸的图标文件，放在项目根目录：

| 文件名 | 尺寸 | 用途 |
|--------|------|------|
| icon16.png | 16x16 像素 | Chrome 工具栏小图标 |
| icon48.png | 48x48 像素 | 扩展管理页面图标 |
| icon128.png | 128x128 像素 | Chrome Web Store 图标 |

**临时方案**：在准备好正式图标之前，可以暂时注释掉 `manifest.json` 中的 `icons` 配置，插件仍可正常运行。

---

## 🔧 项目结构说明

### 为什么这样设计？

1. **manifest.json 在根目录**：Chrome 插件加载时需要从根目录读取配置文件
2. **extension/ 文件夹**：存放插件专用的弹出菜单文件，保持结构清晰
3. **核心文件共享**：`tester.html`、`tester.css`、`tester.js` 同时被：
   - GitHub Pages 网站使用（通过 index.html 跳转）
   - Chrome 插件使用（通过 popup 打开）
4. **图标在根目录**：方便 GitHub Pages 和 Chrome 插件都能访问

### 文件职责说明

| 文件 | 用途 |
|------|------|
| index.html | 官网首页，SEO 落地页，引导用户 |
| tester.html | 核心测试页面，两个平台共用 |
| tester.css | 测试页面样式 |
| tester.js | 测试逻辑，支持 URL 参数 |
| manifest.json | Chrome 插件配置 |
| extension/popup.html | 插件弹出菜单 UI |
| extension/popup.js | 插件菜单逻辑 |

---

## 📝 注意事项

1. **GitHub Pages**：
   - 免费托管，支持自定义域名
   - 只支持静态网站
   - 更新后需要 1-2 分钟生效

2. **Chrome 插件**：
   - 开发者账号需要 $5 一次性费用
   - 审核通常需要 1-3 天
   - 更新版本也需要重新审核

3. **图标文件**：
   - 必须是 PNG 格式
   - 建议使用透明背景
   - 设计风格要简洁专业

---

## 🆘 常见问题

### Q: GitHub Pages 网站无法访问？
A: 检查以下几点：
- 确保仓库是公开的
- Pages 设置是否正确
- 等待 2-5 分钟让部署完成

### Q: Chrome 插件加载失败？
A: 检查：
- manifest.json 格式是否正确
- 所有引用的文件路径是否正确
- 开发者模式是否开启

### Q: 如何更新网站或插件？
A: 
- 网站：push 代码到 GitHub，Pages 会自动更新
- 插件：修改代码后重新打包 zip，在开发者控制台上传新版本
